# RAG and OCR dependencies for NHAI Circular Indexer
# Install with: pip install -r requirements-rag.txt

# Core LangChain
langchain>=0.1.0
langchain-community>=0.1.0
langchain-text-splitters>=0.1.0
chromadb>=0.4.0

# Free embedding models (alternative to OpenAI)
sentence-transformers>=2.2.0

# OCR dependencies
easyocr>=1.7.0
PyMuPDF>=1.23.0
Pillow>=10.0.0

# Document processing
unstructured[all-docs]>=0.10.0

# File type detection (platform-specific)
python-magic>=0.4.27; sys_platform != "win32"
python-magic-bin>=0.4.14; sys_platform == "win32"

# Additional utilities
numpy>=1.24.0

# System dependencies for Linux (install separately):
# Ubuntu/Debian: sudo apt-get install libmagic1
# CentOS/RHEL: sudo yum install file-libs
# macOS: brew install libmagic
